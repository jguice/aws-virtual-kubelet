apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: virtual-kubelet
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: virtual-kubelet
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
