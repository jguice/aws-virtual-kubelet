apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - configmap.yaml
  - statefulset.yaml

commonLabels:
  app.kubernetes.io/name: aws-virtual-kubelet
  app.kubernetes.io/part-of: aws-virtual-kubelet
